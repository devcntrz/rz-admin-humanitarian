/*
Eksekusi schema + seed:

1) Pastikan env:
   - DATABASE_URL=postgres://user:pass@host:port/db
   - DB_CLIENT=neon  (untuk Neon Serverless)
     atau
   - DB_CLIENT=pg    (untuk Postgres native/self-hosted)

2) Jalankan scripts/001_init_schema.sql pada database Anda.
   File ini berisi seluruh DDL + DML contoh.
*/
